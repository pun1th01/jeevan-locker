node_modules
dist
.env
.DS_Store
build
coverage
uploads

# Hardhat build output: regenerated by `hardhat compile` (and by `npm test`, which compiles before deploying
# to its own node). Tracking it made every compile dirty the tree.
server/artifacts/
server/cache/
